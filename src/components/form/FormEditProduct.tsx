import React from 'react';
import { productType } from '../../types/Products'

export const EditProductForm = ({ product }: { product: productType }) => {
  return (
    <form>
      {/* Aquí va el formulario para editar el producto */}
    </form>
  );
};