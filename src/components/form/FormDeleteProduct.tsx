import React from 'react';
import { productType } from '../../types/Products'

export const DeleteProductForm = ({ product }: { product: productType }) => {
  // Lógica para eliminar el producto
  return (
    <form>
      {/* Aquí va el formulario para eliminar el producto */}
    </form>
  );
};